#include <stdlib.h>

typedef struct {
  // Заполнить структуру в соответствии с зададнием
} IntArray;

IntArray CreateNullArray(int size) {
  // ...
//   return новый массив 
}

void PrintIntArray(IntArray array) {
  //
}

int main() {
  // пишем, что тестируем
  int size_0 = 10;
  printf("Create 0-array with size %d", size_0);
  // тестируем  - запускаем CreateNullArray
  // выводим результат: для анализа глазами, правильно ли сработала функция - запускаем PrintIntArray
  
}
